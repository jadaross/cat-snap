// Cat-Snap build configuration — TEMPLATE
// Copy this file to CatSnap.xcconfig (gitignored) and fill in real values.
//
// Note: xcconfig treats "//" as a comment, so URLs must escape the slashes
// using $(). The substitution evaluates to an empty string, leaving "//" intact.

SUPABASE_URL = https:/$()/wgtjtvxpxalyeukgxbpo.supabase.co
SUPABASE_ANON_KEY = your-publishable-key-here
